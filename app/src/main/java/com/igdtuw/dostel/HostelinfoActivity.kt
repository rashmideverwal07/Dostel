package com.igdtuw.dostel

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity

class HostelinfoActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_hostelinfo)

        // Everything is static in layout, so no additional code needed.
        // But you can add navigation or buttons if required later.
    }
}
